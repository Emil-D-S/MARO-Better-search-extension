console.log("Content.js ran");
/*setTimeout(() => {
    console.log("Maro better search started")

    const products = document.querySelectorAll(".product");
    console.log(`Found ${products.length} products`);

    for(const p of products) {
        const title = p.querySelector(".product__title");

        if(title) {
            console.log(title.textContent.trim()); // Use textContent to get the text content
        } else {
            console.log('Title element not found in product.');
        }
    }
}, 1000); // Delay to allow elements to load
*/